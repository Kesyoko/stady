4月11日
学習時間　約2時間
学んだこと
・Gemとは
GemとはRubyのパッケージ管理システム。これを使用すると時間がかかるものが容易に実装しやすくなる。
フレームワークを支えるネジのようなもの。

・bundlerとは
bundlerとは、依存関係を管理するツールのこと。これを使用すると開発環境の一貫性を保ってくれ、複数人で開発が可能となる。

Gemに追記した後は、Dockerを停止させ、再起動させる必要がある。
この操作はCtrl＋Cでは不十分で、docker compose down で停止させ、docker compose upを行わないと変更は反映されない。